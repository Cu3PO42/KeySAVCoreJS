{
  "name": "keysavcore",
  "version": "0.21.0",
  "description": "A library for breaking the encryption of Generation 6 and 7 Pokémon games.",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "tsc --pretty",
    "generate-documentation": "typedoc --mode file --tsconfig tsconfig.json --excludePrivate --excludeNotExported --excludeExternals --exclude util --theme minimal --name KeySAVCore --out doc"
  },
  "author": "Cu3PO42",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^9.3.0",
    "fs-extra": "^5.0.0",
    "mocha": "^4.1.0",
    "typedoc": "^0.9.0",
    "typescript": "^2.6.0"
  }
}
