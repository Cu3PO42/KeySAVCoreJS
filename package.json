{
  "name": "keysavcore",
  "version": "0.19.4",
  "description": "A library for breaking the encryption of Generation 6 and 7 Pokémon games.",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "tsc && tsc --declaration",
    "generate-documentation": "typedoc --mode modules --tsconfig tsconfig.json --excludePrivate --excludeNotExported --excludeExternals --exclude util --theme minimal --name KeySAVCore --out doc"
  },
  "author": "Cu3PO42",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^6.0.48",
    "fs-extra": "^0.26.5",
    "mocha": "^2.4.5",
    "typedoc": "^0.8.0",
    "typescript": "^2.0.10"
  }
}
